import { NextRequest, NextResponse } from 'next/server';export async function GET(req:NextRequest,{params}:{params:{slug:string}}){const {searchParams}=new URL(req.url);const format=searchParams.get('format');if(format==='faqs'){return NextResponse.json({'@context':'https://schema.org','@type':'FAQPage','mainEntity':[{'@type':'Question','name':'What is Example?','acceptedAnswer':{'@type':'Answer','text':'A sample entity.'}}]});}return NextResponse.json({slug:params.slug,lastModified:new Date().toISOString(),verificationLevel:0,data:{brandName:'Example',tagline:'Structured facts for AI',website:'https://example.com'},jsonLd:[{'@context':'https://schema.org','@type':'Organization','name':'Example','url':'https://example.com'}]});}